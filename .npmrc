# The Zoom Meeting SDK (@zoom/meetingsdk) declares a hard peer of react@18.2.0,
# but this app runs React 19. The SDK works fine at runtime, so allow npm
# (including CI's `npm clean-install`) to resolve past the peer conflict instead
# of failing with ERESOLVE.
legacy-peer-deps=true
