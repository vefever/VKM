# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist.zip
*.zip
.output
.vinxi
.tanstack/**
.nitro
*.local

# Env files (contain the Supabase service-role secret — never commit)
.env
.env.*
!.env.example

# Local Claude settings can hold one-off secrets pasted into command allowlists
.claude/settings.local.json
# Supabase CLI scratch (may cache project refs / db URLs)
supabase/.temp/

# Local terminal session logs (auto-recorded scratch, not app source)
terminals/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.dev.vars*
!.dev.vars.example
